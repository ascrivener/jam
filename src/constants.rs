pub const NUM_VALIDATORS: usize = 1023;
pub const NUM_VALIDATOR_SAFETY_THRESHOLD: usize = (2 * NUM_VALIDATORS as usize) / 3 + 1;
pub const NUM_TICKET_ENTRIES: u32 = 2;
pub const NUM_TIMESLOTS_PER_EPOCH: usize = 600;
pub const NUM_CORES: u32 = 341;
pub const AUTHORIZER_QUEUE_LENGTH: u32 = 80;
